<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ba.sum.fpmoz.mim">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ednevnik">
        <activity android:name=".HomeNavigationActivity"></activity>
        <activity android:name=".UserAdminEditActivity" />
        <activity android:name=".UserMainActivity" />
        <activity
            android:name=".TabbedUserAdminActivity"
            android:label="@string/title_activity_tabbed_user_admin"
            android:theme="@style/Theme.Ednevnik.NoActionBar" />
        <activity android:name=".UserAdminActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>